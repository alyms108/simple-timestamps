'use babel';

import SimpleTimestampsView from '../lib/simple-timestamps-view';

describe('SimpleTimestampsView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
